const button = document.getElementById("button");
const text = document.getElementById("text");

function onClicked() {
  text.textContent = "Не сумуй, ты дуже класна😊)";
}
